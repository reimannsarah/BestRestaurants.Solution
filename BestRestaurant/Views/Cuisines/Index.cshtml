@{
  Layout = "_Layout";
}

@using BestRestaurant.Models

@if (Model.Count == 0)
{
  <p>pffff, no cuisines just farts</p>
}

@foreach (Cuisine cuisine in Model)
{
  <p>@Html.ActionLink($"{cuisine.Type}", "Details", new{ id = cuisine.CuisineId })</p>
}

